-- Migration to clean up duplicate columns in the users table
-- SQLite-compatible version (columns already cleaned up)

-- This migration is a no-op for SQLite as the columns were already handled
-- in the previous migration

SELECT 1;
